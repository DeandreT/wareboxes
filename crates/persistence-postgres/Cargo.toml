[package]
name = "wareboxes-persistence-postgres"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Wareboxes PostgreSQL connection, migration, and persistence primitives"

[dependencies]
wareboxes-application = { workspace = true }
wareboxes-domain = { workspace = true }
wareboxes-worker = { workspace = true }
chrono = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = "0.10"
thiserror = { workspace = true }
anyhow = { workspace = true }
async-trait = "0.1"
sqlx = { version = "0.7", default-features = false, features = [
    "runtime-tokio",
    "macros",
    "postgres",
    "chrono",
    "migrate",
] }
